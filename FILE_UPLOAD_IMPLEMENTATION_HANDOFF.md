# 大文件上传功能需求与接口设计建议

> 本文用于向开发人员说明大文件上传功能需要实现什么，以及一种可供参考的整体方案和接口设计。
>
> 文档不要求复刻某个现有项目的代码结构。具体类设计、存储组件和技术实现应结合公司项目现状决定。

## 1. 功能背景与范围

### 1.1 业务场景

系统需要支持上传体积较大的图片 ZIP 文件。文件可能达到 GB 甚至更高量级，不能依赖单次普通 multipart 请求完成上传。

上传功能应通过分片、重试和断点续传降低大文件传输失败后的重传成本，并在上传完成后向后续业务处理流程提供一份完整、可信的文件。

公司当前采用单实例服务，因此本文以“单实例应用 + 本地持久化文件系统”为默认部署前提，不额外引入多实例之间的状态协调和共享存储要求。

### 1.2 建设目标

- 支持大文件分片上传。
- 支持分片完整性校验和失败重试。
- 支持页面刷新或网络中断后的断点续传。
- 支持查询任务状态和上传进度。
- 支持完成上传、合并分片和校验最终文件。
- 支持主动取消和过期任务清理。
- 支持将完整文件交给后续业务处理模块。
- 支持应用重启后的任务恢复或明确失败处理。

### 1.3 功能边界

本文所说的“上传功能”负责：

```text
创建任务
  -> 接收分片
  -> 校验分片
  -> 断点续传
  -> 合并并校验完整文件
  -> 将文件交给后续业务处理器
```

上传后的图片解压、图片格式识别、内容判重和业务入库属于后续业务处理。上传模块只需要定义稳定的交接入口，并允许进度接口展示后续处理状态，不规定其内部实现。

以下内容不在本文范围内：

- 历史图片数据回填。
- 服务器已有目录扫描或导入。
- 图片标注等后续业务流程。
- Docker、Nginx 和具体部署命令。
- 具体 Java 类、数据库 Mapper 或前端组件代码。

本文使用以下措辞区分约束程度：

- **必须**：功能验收需要满足。
- **推荐**：默认实现方向，可以根据公司基础设施调整。
- **可选**：根据产品体验和运行规模决定。

## 2. 功能需求

### 2.1 创建上传任务

客户端在发送分片前必须先创建上传任务，并提交必要的文件元数据，例如：

- 原始文件名。
- 文件总大小。
- 文件类型或业务类型。
- 可选的完整文件 SHA-256。

服务端必须：

- 校验文件类型、大小和用户权限。
- 生成不可预测的任务标识 `uploadId`。
- 记录任务创建者，后续操作必须校验任务归属。
- 返回分片大小、总分片数和任务过期时间。

上传责任人应从登录上下文获取，不应直接信任客户端提交的用户名。

### 2.2 上传分片

客户端根据服务端约定的分片大小切分文件，并按照从 `0` 开始的序号上传。

每个分片请求必须包含：

- `uploadId`。
- `chunkIndex`。
- 分片内容。
- 分片 SHA-256。

服务端必须：

- 校验任务存在且属于当前用户。
- 校验任务仍允许接收分片。
- 校验 `chunkIndex` 在合法范围内。
- 流式接收分片，避免一次性读取到内存。
- 校验服务端计算出的摘要与客户端摘要一致。
- 只在完整写入并校验成功后确认该分片已上传。

同一分片重复上传必须具有幂等语义，不能导致已上传数量重复累计。

### 2.3 查询分片与断点续传

服务端必须提供已上传分片查询接口，返回明确的分片序号列表。

客户端恢复上传时应：

1. 确认用户重新选择的是同一个原文件。
2. 查询服务端已上传分片序号。
3. 只补传缺失分片。

不能只返回“已上传 N 个分片”，因为客户端无法据此判断具体缺少哪些序号。

### 2.4 完成上传

所有分片上传结束后，客户端调用完成接口。

服务端必须：

- 检查所有分片是否齐全。
- 检查分片大小和摘要记录是否一致。
- 按正确顺序生成完整文件。
- 校验合并后文件大小。
- 如果客户端提供了完整文件 SHA-256，校验最终文件摘要。
- 防止重复请求触发多个合并任务。

合并可能耗时较长，推荐作为后台任务执行。完成接口只负责受理请求并快速返回当前状态。

### 2.5 查询任务状态

客户端必须能够通过 `uploadId` 查询：

- 当前任务状态。
- 总分片数和已上传分片数。
- 当前处理阶段。
- 失败时的错误码和简要原因。
- 可选的后续业务处理进度。

前端应区分“分片已经传完”和“整个任务已经处理完成”，不能在分片上传结束后直接显示任务完成。

### 2.6 取消任务

客户端应支持取消尚未完成的上传任务。

取消任务时必须：

- 停止接收新的分片。
- 清理已上传分片和临时文件。
- 将任务标记为 `CANCELED`。
- 保留必要的任务审计信息。

取消接口推荐具备幂等性，重复取消同一个任务不应产生额外错误。

任务进入合并或后续处理阶段后是否允许取消，应根据公司业务的数据一致性要求决定。

### 2.7 过期清理

系统必须为未完成任务设置过期时间，并定期清理：

- 长时间未更新的上传任务。
- 遗留分片。
- 合并失败产生的不完整文件。
- 后台处理异常遗留的临时文件。

任务元数据过期和物理文件清理必须协同完成，不能只清理其中一方。

### 2.8 交给后续业务处理

完整文件校验成功后，上传模块应调用统一的业务处理入口，例如：

```text
process(uploadId, fileLocation, businessContext)
```

该入口可以由图片 ZIP 导入、视频转码或其他业务处理器实现。上传模块只关心任务是否被成功受理，以及处理器回写的状态和进度。

## 3. 推荐实现方案

### 3.1 推荐主流程

```text
客户端选择文件
  -> 创建上传任务
  -> 按有限并发上传分片
  -> 查询并补传缺失分片
  -> 提交完成上传
  -> 后台合并和校验完整文件
  -> 提交给后续业务处理器
  -> 客户端查询任务状态直至结束
```

推荐把上传过程建模为一个长期任务，而不是若干互不关联的文件请求。

### 3.2 任务信息

上传任务至少需要保存以下信息：

- `uploadId`。
- 创建者和所属业务。
- 原始文件名和总大小。
- 分片大小和总分片数。
- 已上传分片信息。
- 当前状态。
- 完整文件存储位置和可选摘要。
- 错误码和错误信息。
- 创建、更新时间和过期时间。

这些是任务模型所需的信息，不代表必须按照某种固定表结构实现。

### 3.3 状态与文件存储

结合公司当前的单实例部署方式，推荐：

- 使用数据库持久化任务状态，保证应用重启后仍能识别未完成任务。
- 使用本地持久化文件系统保存分片和合并文件。
- 上传目录必须位于明确的持久化数据目录，不能依赖会随进程或容器销毁的临时目录。
- 提前规划磁盘容量、目录权限、空间告警和过期文件清理。
- 如果公司本身已有对象存储，仍可根据磁盘容量和运维成本选择使用，但它不是当前单实例方案的必要条件。

当前没有必要为了上传功能额外引入 Redis、共享文件系统或分布式存储。未来如果服务扩展为多实例，再重新评估状态共享和文件共享方案。

### 3.4 分片策略

推荐以 `32MB～64MB` 作为初始分片大小，再根据网关限制、网络质量和压测结果调整。

客户端推荐限制并发数，例如同时上传 `3` 个分片，避免一次性发出全部请求。

分片大小应由服务端统一返回。这样服务端可以根据环境限制调整，客户端不需要把固定值写死。

### 3.5 完整性校验

推荐使用 SHA-256：

- 每个分片必须提供 SHA-256，用于发现传输损坏。
- 完整文件 SHA-256 推荐提供，用于确认分片组合后仍是原文件。

摘要只用于完整性校验和文件身份确认，不应代替用户权限校验。

### 3.6 幂等与并发控制

以下操作需要明确的幂等或并发语义：

- 重传同一个分片。
- 并发上传同一个分片。
- 重复调用完成接口。
- 重复取消任务。
- 后台工作进程重复消费同一个任务。

单实例并不代表没有并发请求。推荐使用任务状态条件更新、唯一约束、乐观锁或应用内按 `uploadId` 加锁来保证一致性；当前不需要为此额外引入分布式锁。

### 3.7 前端协作

前端负责：

- 文件选择和基础校验。
- 按服务端返回的大小切片。
- 有限并发上传。
- 分片级失败重试。
- 保存 `uploadId` 和文件身份信息。
- 查询已上传分片并恢复任务。
- 展示上传阶段和后台处理阶段。

上传进度可以在浏览器本地按已传字节计算；合并和后续处理进度应以服务端状态为准。

## 4. 推荐接口设计

以下接口以 `/api/uploads` 为示例。实际路径可以根据公司项目的 API 规范调整。

### 4.1 接口总览

| 功能 | 方法与路径 | 推荐成功状态 |
| --- | --- | --- |
| 创建上传任务 | `POST /api/uploads` | `201 Created` |
| 上传分片 | `PUT /api/uploads/{uploadId}/chunks/{chunkIndex}` | `200 OK` |
| 查询已上传分片 | `GET /api/uploads/{uploadId}/chunks` | `200 OK` |
| 完成上传 | `POST /api/uploads/{uploadId}/complete` | `202 Accepted` |
| 查询任务状态 | `GET /api/uploads/{uploadId}` | `200 OK` |
| 取消任务 | `DELETE /api/uploads/{uploadId}` | `204 No Content` |

### 4.2 创建上传任务

```http
POST /api/uploads
Content-Type: application/json
```

```json
{
  "originalFilename": "dataset.zip",
  "totalSize": 1099511627776,
  "fileSha256": "可选的完整文件摘要",
  "businessArea": "medical"
}
```

推荐响应：

```json
{
  "uploadId": "0bfca031-8cf7-428f-89a8-7d93dba6b35d",
  "status": "CREATED",
  "chunkSize": 67108864,
  "totalChunks": 16384,
  "expiresAt": "2026-07-29T10:00:00+08:00"
}
```

服务端根据 `totalSize` 和 `chunkSize` 计算 `totalChunks`。如果公司前端已经负责确定分片大小，也可以由客户端提交 `totalChunks`，但服务端和客户端之间应只有一个明确的数据来源。

### 4.3 上传分片

```http
PUT /api/uploads/{uploadId}/chunks/{chunkIndex}
Content-Type: multipart/form-data
```

```text
file=@chunk.bin
checksum=<当前分片 SHA-256>
```

推荐响应：

```json
{
  "uploadId": "0bfca031-8cf7-428f-89a8-7d93dba6b35d",
  "chunkIndex": 7,
  "uploadedChunks": 8,
  "totalChunks": 16384
}
```

`PUT` 表达“为某个确定的分片序号写入内容”，便于定义重复请求的幂等语义。

### 4.4 查询已上传分片

```http
GET /api/uploads/{uploadId}/chunks
```

```json
{
  "uploadId": "0bfca031-8cf7-428f-89a8-7d93dba6b35d",
  "status": "UPLOADING",
  "totalChunks": 16384,
  "uploadedChunkIndexes": [0, 1, 2, 4, 7]
}
```

前端将 `uploadedChunkIndexes` 转换为集合，只上传不在集合中的分片。

### 4.5 完成上传

```http
POST /api/uploads/{uploadId}/complete
```

推荐响应：

```json
{
  "uploadId": "0bfca031-8cf7-428f-89a8-7d93dba6b35d",
  "status": "MERGING"
}
```

接口推荐立即返回 `202 Accepted`。分片检查、合并和最终摘要校验由后台任务执行。

### 4.6 查询任务状态

```http
GET /api/uploads/{uploadId}
```

```json
{
  "uploadId": "0bfca031-8cf7-428f-89a8-7d93dba6b35d",
  "status": "PROCESSING",
  "uploadedChunks": 16384,
  "totalChunks": 16384,
  "currentStage": "图片 ZIP 处理中",
  "processingProgress": 72,
  "errorCode": null,
  "errorMessage": null,
  "updatedAt": "2026-07-27T10:10:00+08:00"
}
```

`currentStage` 和 `processingProgress` 可以由后续业务处理器提供。如果上传模块只负责生成完整文件，这两个字段可以省略。

### 4.7 取消任务

```http
DELETE /api/uploads/{uploadId}
```

成功返回：

```http
204 No Content
```

推荐保留任务记录并将其状态改为 `CANCELED`，物理文件可以同步或异步清理。

### 4.8 错误响应

推荐沿用公司统一错误响应。如果没有现成规范，可以基于 `ProblemDetail` 增加稳定的业务错误码：

```json
{
  "status": 409,
  "title": "Upload state conflict",
  "detail": "当前任务已进入合并阶段，不能继续上传分片",
  "errorCode": "UPLOAD_STATE_CONFLICT",
  "requestId": "trace-id"
}
```

建议至少区分：

| 场景 | 推荐状态码 |
| --- | --- |
| 请求参数错误 | `400` |
| 未登录或无任务权限 | `401/403` |
| 上传任务不存在 | `404` |
| 任务状态冲突 | `409` |
| 分片摘要不匹配 | `422` |
| 单个请求或文件超过限制 | `413` |
| 请求过于频繁 | `429` |
| 存储或后台服务暂时不可用 | `503` |

## 5. 任务状态约定

### 5.1 推荐状态

| 状态 | 含义 |
| --- | --- |
| `CREATED` | 任务已创建，尚未收到有效分片 |
| `UPLOADING` | 正在接收分片 |
| `MERGING` | 分片已齐全，正在生成和校验完整文件 |
| `PROCESSING` | 完整文件已交给后续业务处理器 |
| `DONE` | 整个任务已完成 |
| `FAILED` | 上传、合并或后续处理失败 |
| `CANCELED` | 用户取消或系统终止任务 |

如果公司只需要上传完整文件、不需要追踪后续处理，可以省略 `PROCESSING`，在完整文件校验成功后直接进入 `DONE`。

### 5.2 正常状态流转

```text
CREATED -> UPLOADING -> MERGING -> PROCESSING -> DONE
   |           |           |            |
   +-----------+-----------+------------+-> FAILED
   |           |
   +-----------+--------------------------> CANCELED
```

状态转换必须由服务端控制。客户端只能发起操作，不能直接指定任务状态。

### 5.3 状态操作规则

- 只有 `CREATED`、`UPLOADING` 可以继续上传分片。
- 只有分片齐全的任务可以进入 `MERGING`。
- `complete` 重复调用不能启动多个合并任务。
- `DONE` 和 `CANCELED` 是终态。
- `FAILED` 是否允许原任务重试，应根据失败阶段决定；无法安全恢复时创建新任务。
- 是否允许取消 `MERGING` 或 `PROCESSING`，由业务处理器的一致性能力决定。

## 6. 非功能要求

### 6.1 性能与资源

- 文件和分片必须流式读写，不能整体加载到 JVM 内存。
- 必须限制单文件大小、分片数量、单用户并发任务数和总存储配额。
- 合并和后续处理不能长期占用 HTTP 请求线程。
- 应根据真实网络、本地磁盘和后台处理性能进行压测。

### 6.2 数据完整性

- 分片校验失败不能被记录为上传成功。
- 合并前必须确认分片序号连续且数量正确。
- 合并失败不能留下被误认为成功的完整文件。
- 任务状态、分片状态和物理文件之间必须有清理或补偿机制。

### 6.3 安全

- 所有接口必须校验任务创建者或所属租户。
- `uploadId` 和存储路径不能直接使用未经校验的外部输入。
- 原始文件名只能作为展示信息，不能直接作为服务器存储路径。
- 如果后续处理会解压 ZIP，必须限制条目数、单条目大小、总展开大小和路径穿越。
- 根据公司要求增加病毒扫描或内容安全检查。

### 6.4 单实例可靠性

- 上传工作目录必须使用持久化磁盘，不能依赖进程生命周期内的临时空间。
- 后台任务需要防止重复消费。
- 应用重启后，未完成任务必须能够恢复、重试或明确进入失败状态。
- 应用启动时应能够根据任务记录和磁盘文件识别未完成或异常任务。
- 本地磁盘空间不足或目录不可写时，应返回可识别的错误并触发告警。

### 6.5 清理与保留

- 未完成任务需要明确过期时间。
- `DONE`、`FAILED`、`CANCELED` 的中间文件需要不同的保留策略。
- 定时清理任务必须同时处理元数据和物理文件。
- 清理过程应可重试，并记录失败情况。

### 6.6 可观测性

推荐记录：

- 上传任务数量和成功率。
- 分片失败与重试次数。
- 上传、合并和后续处理耗时。
- 当前临时文件占用空间。
- 后台任务队列长度。
- 按错误码统计的失败数量。

日志应包含 `uploadId`、用户或租户标识和请求追踪 ID，但不能记录文件内容或敏感业务数据。

## 7. 验收标准

功能至少满足以下条件才可以认为完成：

1. 大文件能够按服务端约定切片并上传成功。
2. 分片可以乱序上传，最终文件内容仍与原文件一致。
3. 分片摘要不匹配时上传失败，且不会覆盖已有有效分片。
4. 同一分片重复上传不会导致进度重复累计。
5. 页面刷新或网络中断后可以查询已上传分片并继续上传。
6. 文件身份不一致时不能错误复用旧任务。
7. 缺少任一分片时不能完成上传。
8. 重复调用完成接口只会产生一个合并任务。
9. 完整文件大小和可选 SHA-256 校验正确。
10. 用户只能操作自己或本租户的上传任务。
11. 取消任务后不再接收分片，并能够清理临时文件。
12. 过期任务不会长期占用数据库或本地文件存储。
13. 应用重启后，未完成任务能够恢复、重试或进入明确的失败状态。
14. 上传和合并过程不会因为文件体积增长而等比例占用应用内存。
15. 失败响应包含稳定错误码和请求追踪信息。
16. 完整文件能够被可靠地交给后续业务处理器。

本文只确定功能目标、推荐主流程和接口边界。具体包结构、类设计、数据表、本地存储目录和后台任务框架，由开发人员结合公司项目已有规范自行设计。
